export default async function handler(req, res) {
    // Simulate running a JavaScript file.
    // Replace this logic with actual computation or file execution as needed.
    const result = "Hello from JavaScript file!";
    res.status(200).send(result);
}